import pandas as pd
import numpy as np

'''
                                Using project checklist 
1) Frame your problem

2) Get the data
    - Download the data
    - Modify data to be representative. stratify sets
    - Create train and test sets
3) 
'''

# sz = 10
# x = list(range(sz))

# def train_test_split(data, train_size = 0.8):
#     size = len(data)
#     train, test = data[:int(size*train_size)], data[int(size*train_size):]
#     return train, test

# print(train_test_split(x))